"""The saddler IRC hub: a self-contained IRC server + web dashboard where humans
and agent bots co-inhabit channels. Built in-house (not ngircd) so the dashboard
can surface app-level agent metadata — role, model, source — that standard IRC
does not carry. Standard IRC clients still connect as humans.
"""
