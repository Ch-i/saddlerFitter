"""Checks viewer — a GitHub-Actions-style surface over saddler runs.

Each flag (finding) is a check: open it to see its action path (propose -> evidence
-> verify -> arbitrate), the agents' thought, the raw terminal/IRC streams, and to
trigger a saddler fix or a grill-me session on that single flag.
"""
